@page "/dashboard/products/{Id:int}"

@using Microsoft.AspNetCore.Components.Authorization

<MudCard>
    <MudCardMedia Image="@productModel.ImageUrl"
                  Height="300"
                  Style="background-size: contain;
                         background-repeat: no-repeat;
                         background-position: center;" />
    <MudCardContent>
        <MudText Align="Align.Center" Typo="Typo.h5">@productModel.Name</MudText>
        <MudText Align="Align.Center" Typo="Typo.subtitle1">@productModel.Description</MudText>
        <MudText Align="Align.Center" Typo="Typo.body1">Category: @_category | Brand: @_brand</MudText>
        <MudText Align="Align.Center" Typo="Typo.body2">Stock: @productModel.Stock | Price: @productModel.Price</MudText>
    </MudCardContent>

    <AuthorizeView Roles="Admin">
        <MudCardActions>
            <MudButton Variant="Variant.Filled"
                       Style="background-color: #C998ED;"
                       OnClick="@(() => DeleteProduct(productModel.Id))">
                @Res.Lang.Btn_Delete
            </MudButton>

            <MudButton Variant="Variant.Filled"
                       Style="background-color: #C998ED;"
                       OnClick="@(() => EditProduct(productModel.Id))">
                @Res.Lang.Btn_Edit
            </MudButton>
        </MudCardActions>
    </AuthorizeView>

    <AuthorizeView Roles="Customer">
        <MudButton Variant="Variant.Filled"
                   Style="background-color: #C998ED; margin-top: 15px;"
                   Disabled="@(productModel.IsActive && productModel.Stock > 0 ? false : true)"
                   OnClick="@(() => AddToCart(productModel))">
            @Res.Lang.Btn_AddToCart
        </MudButton>
    </AuthorizeView>

</MudCard>